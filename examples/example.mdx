import { a } from 'pack';
import { b, c, d } from '@namespace/test';

<Meta title="Test/Page" />

# Header

Some simple text

## Example

export const Example = ({ title }) => {
  return <h2>{title}</h2>;
};

